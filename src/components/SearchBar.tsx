const SearchBar = () => {
  return (
    <input
      type="text"
      className="form-control"
      placeholder="Search games..."
      aria-label="Search"
      aria-describedby="basic-addon2"
    />
  );
};

export default SearchBar;
